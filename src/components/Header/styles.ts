import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    flex-direction: column;
    grid-area: header;
    width: 100%;

`;

export const UpperText = styled.h2`
    
`;

export const Divider = styled.hr`

`;

export const LowerText = styled.p`

`;